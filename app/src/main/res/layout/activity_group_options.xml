<?xml version="1.0" encoding="utf-8"?>
<!-- activity_group_options.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <!-- Button to create a group -->
    <Button
        android:id="@+id/btnCreateGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create a Group"
        android:layout_marginBottom="16dp" />

    <!-- Input field for entering a group code (for joining) -->
    <EditText
        android:id="@+id/inputGroupCode"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Group Code"
        android:inputType="text"
        android:layout_marginBottom="16dp" />

    <!-- Button to confirm joining a group -->
    <Button
        android:id="@+id/btnJoinGroupConfirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Join" />
</LinearLayout>
